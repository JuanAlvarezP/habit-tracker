# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/reference/configuration-reference
version: 2.1

orbs:
  # See the Node orb documentation here: https://circleci.com/developer/orbs/orb/circleci/node
  node: circleci/node@5.2

# Orchestrate jobs using workflows
# See: https://circleci.com/docs/guides/orchestrate/workflows/ & https://circleci.com/docs/reference/configuration-reference/#workflows
workflows:
  sample:
    # Inside the workflow, you define the jobs you want to run.
    jobs:
      - node/test:
          # AÑADE ESTA LÍNEA 👇
          app-dir: habit-tracker-frontend
          
          # Esto le dice al Orb de Node que la aplicación está en este subdirectorio.
          # El Orb ejecutará automáticamente comandos como `npm install` y 
          # las pruebas dentro de este directorio.
          
          version: '16.10'
          # If you are using yarn, change the line below from "npm" to "yarn"
          pkg-manager: npm